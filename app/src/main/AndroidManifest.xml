<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.sales_force">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SincronizaDados"></activity>
        <activity android:name=".ProductAdministrationActivity" />
        <activity android:name=".ClientAdministrationActivity" />
        <activity android:name=".UserAdministrationActivity" />
        <activity android:name=".RegisterOrder" />
        <activity android:name=".RegisterProduct" />
        <activity android:name=".CreateClientActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".CreateUserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>